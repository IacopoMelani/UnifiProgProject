package javamm;

@SuppressWarnings("all")
public class HarmonicSum {
  public static int harmonicSum(int x) {
    double sum = 0;
    int i = 1;
    while ((sum < x)) {
      {
        double _harmonic = HarmonicSum.harmonic(i);
        double _plus = (sum + _harmonic);
        sum = _plus;
        i++;
      }
    }
    return i;
  }
  
  public static double harmonic(int k) {
    int i = 1;
    double sum = 0;
    do {
      {
        sum = (sum + (1.0 / i));
        i++;
      }
    } while((i < k));
    return sum;
  }
  
  public static void main(String[] args) {
    System.out.println(HarmonicSum.harmonicSum(5));
  }
}
